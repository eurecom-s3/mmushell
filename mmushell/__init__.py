"""
OS-Agnostic memory morensics tool

Modules exported by this package:

- `mmushell`: main script allowing to reconstruct virtual address spaces from a memory dump.
- `exporter`: this is a POC showing the possible use of techniques to perform a preliminary analysis of a dump by exporting each virtual address space as a self-contained ELF Core file.
"""
